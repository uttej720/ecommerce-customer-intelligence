# Derived data

Store only reviewed, aggregated, non-identifying result files here.

Examples:
- customer_risk_categories.csv
- rfm_segment_summary.csv
- geographic_delay_exposure_summary.csv

Never add raw transactions, customer-level exports, names, emails, addresses, passwords, or database backups.